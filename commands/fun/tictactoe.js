/*const { tictactoe } = require('reconlx');

module.exports = {
        name: "tictactoe",
        category: 'fun',
        aliases: ['ttt'],

        run: async (client, message, args) => {
                const member = message.mentions.members.first()
                if(!member) return message.channel.send('Pls specify a user to play again!')

                new tictactoe({
                        player_two: member,
                        message: message
                })
        }
}*/